[package]
name = "exelock-pe"
version = "0.1.0"
edition = "2021"

[features]
default = []
# RunPE 内存加载器（stub 专用，packer 不需要）
loader = ["dep:windows"]

[dependencies]
goblin = { workspace = true }
thiserror = { workspace = true }
windows = { workspace = true, optional = true }
