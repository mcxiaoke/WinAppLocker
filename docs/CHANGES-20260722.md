# 变更记录 2026-07-22

## 2026-07-22 16:50 新增单元测试工程（C# + C host 模式）

### C# 测试工程（dotnet/tests/WinAppLocker.{Shared,Packer}.Tests/）
- 新建 `WinAppLocker.Shared.Tests`（xUnit + net472）+ `WinAppLocker.Packer.Tests`
- `WinAppLocker.slnx` 加 `/tests/` 文件夹
- `packer/WinAppLocker.Packer.csproj` 加 `<InternalsVisibleTo>` 让测试访问 internal 类
- `dotnet/tests/nuget.config` 显式禁用本地不存在的 `LocalPackages` 源

### C# 测试覆盖（101 个用例，全通过）
- **P0 共享层**：`Crc32`+`LE`（11 用例，含 IEEE 802.3 标准向量 `"123456789"→0xCBF43926`）、`PayloadHeader`/`Footer`（19 用例，含 magic/CRC 篡改检测）、`PayloadFormat` 常量（5 用例）
- **P0 packer 层**：`CryptoUtil`（13 用例，含 RFC 7914 PBKDF2-HMAC-SHA256 向量 `"passwd"/"salt"/c=1`）
- **P1 共享层**：`ExtTlv`（12 用例，含 UTF-8 中文/emoji）、`ByteSearch`（11 用例，含 length 边界）
- **P1 packer 层**：`PayloadBuilder`（15 用例，含完整字节布局 + ExtTlv 解析往返）、`PeReader`（15 用例，用 `temp/samples/` 实际 PE 样本验证 x64/x86/.NET/Chromium 检测）

### C 测试工程（packer/tests/host/）
- 新建 `host_sha256_test.c` + `host_xtea_test.c` + `host_tests.ps1`（构建脚本）
- 不接入 CMake（避免影响现有 WINLOCK_ARCH 配置），独立 host 模式编译
- 不定义 `WINLOCK_PIC` → `common/sha256.h`/`xtea.h` 走 host 分支（普通 static 函数）

### C 测试覆盖（52 个用例，全通过）
- **SHA-256**（18 用例）：NIST 标准向量（空串、`"abc"`、`"hello"`、>64 字节多块）、update 分块 vs 一次性、`bytes_eq_const` 常量时间比较、`utf16le_to_utf8`（ASCII/中文/空串）
- **XTEA**（34 用例）：单块 encrypt/decrypt 往返、缓冲区往返（size=0/1/2/7/8/9/15/16/17/31/32/100/256）、尾部异或逻辑、全 0/全 0xFF、不同 key 不同密文、同 key 确定性

### 历史教训
- SHA-256 原实现 `K[52]` 曾错写为 `0x5b9cca5f`（正确 `0x5b9cca4f`），导致全部哈希错误 —— NIST 标准向量测试可立即捕获此类回归
- `CryptoUtil` 是 packer 端独立实现，与 stub 端 `dotnet/stub/CryptoUtil.cs` 要求字节级一致，RFC 7914 向量测试是捕获格式漂移的最强保障

---

## 2026-07-22 23:50 dotnet stub 重命名 + GUI 禁用逻辑放宽 + info panel 优化

### stub 名称重命名（5 个 meta JSON）
- `applocker-gui` → `gui`，`applocker-console` → `cli`，`applocker-test` → `test`
- `winlock` → `inplace`，`winlock-reflective` → `reflective`
- 同步更新 `build.ps1`（meta 生成）、`auto_test.ps1`（`--stub-name inplace`）、`Program.cs` 帮助文本、`README.md`、代码注释

### GUI 禁用逻辑放宽（`MainForm.cs` + `PackCore.cs`）
- inplace / reflective 模式仅对 .NET 程序禁用，移除 Console 子系统和 Chromium 系浏览器的禁用判断
- C builder（builder.c / builder_reflective.c）原本就只检查 .NET CLR，无 Console/Chromium 限制
- `PackCore.cs` 防御性 throw 同步移除 Console/Chromium 检查，保留 .NET 检查
- `LoadStubList()` 移除 "(兼容性较低)" 和 "(内存加载，支持 Console/x86)" 标签

### GUI info panel 布局优化（`MainForm.Designer.cs` + `MainForm.cs`）
- 3 个 stub 版本 label 合并为 1 个多行 `lblStubInfo`，每个 stub 一行
- 第一行：Packer 版本 + git hash + 构建时间
- 后续行：每个 stub 显示 `名称 版本 [架构] 类型 可用性`
- 界面扩大：737×581 → 860×680，文本框/按钮/进度条同步加宽
- 修复 cbStubPreference 与 lblIterations 重叠问题

### builder 架构校验确认
- inplace builder：`verify_stub_identity()` 校验 `identity.stub_arch` 字段（编译期注入）
- reflective builder：校验 PE `FileHeader.Machine` 字段
- 两者失败均 `return 1` 退出，无自动切换

### 测试结果
- tempfile 模式：5/5 通过（2 样本缺失跳过）
- WinLock (inplace) 模式：4/4 通过（hellomingw/helloucrt Console 程序正常加壳运行，hellowinforms .NET 正确拒绝）

## 2026-07-22 23:30 代码审查清理第二轮 + CMake VS 自动探测（hy3 N4/N6/N10 + ds4p Q2/F1）

按 review 清单处理 8 项，22/22 e2e 测试通过。

### 改动列表

**CMake VS 自动探测**（`cmake/msvc_setup.cmake` + `build.ps1`）
- 用 `vswhere.exe`（VS 自带标准工具）自动探测最新 VS 安装路径
- 推导 vcvars64.bat / vcvarsall.bat 路径，不再写死绝对路径
- 探测失败时 fallback 到默认 Community 版路径
- 用户可用 `-DWINLOCK_VCVARS64=<path>` 覆盖

**N4 显式传入输入 PE 基址**（`inplace/builder.c`）
- 保存 `input_image_base` 局部变量（PE 解析后立即保存）
- `sd->image_base = input_image_base`，不依赖 `g_nt` 全局状态
- 消除"1043 行调用顺序变化导致读到 out 缓冲区基址"的隐性 bug

**N6 常量化魔法数字**（`reflective/loader.c` + `inplace/builder.c` + `reflective/builder_reflective.c`）
- loader.c 顶部定义常量：
  - `VE_CODE_DBG_PRINT / VE_CODE_SET_THREADNAME / VE_CODE_CPP_EXCEPTION`（VEH 异常码）
  - `SIZE_OF_IMAGE_MAX / SIZE_OF_IMAGE_FALLBACK`（SizeOfImage 校验）
  - `USER_ADDR_MIN / USER_ADDR_MAX`（image_base 合法区间）
  - `DBG_DEFAULT_IMG_BASE / DBG_DEFAULT_IMG_END`（调试用默认基址区间）
- builder*.c 加 `INPUT_FILE_SIZE_MAX`（512MB 文件大小上限）
- 替换全部裸魔法数字（0x40010006 / 64*1024*1024 / 0x400000-0x500000 等）

**Q2 build_dialog MSVC 路径走结构体直填**（`inplace/stub.c`）
- `#ifdef _MSC_VER` 分支用 `DLGTEMPLATE* / DLGITEMTEMPLATE*` 结构体直填
- MinGW 路径保留 `put_dword/put_word` 逐字段写入 hack（避免 -O2 常量池合并）
- `put_word / put_wstr / align_dword` 两条路径共用，`put_dword` 仅 MinGW 需要
- MSVC 路径代码更清晰，不再需要 WINLOCK_OPTIMIZE_OFF + put_dword hack

**F1 明确构建工具职责**（`packer/README.md`）
- 补充"构建工具职责"说明：build.ps1 唯一入口，CMakeLists.txt 底层脚本，Makefile.mingw DEPRECATED
- 修正 README 中 `.lock` → `.text2` 旧节名

**N10 stub 节布局契约文档**（`packer/docs/STUB_LAYOUT.md` 新建）
- stub_data_t 字段表（v6, 192 字节，含偏移/大小/类型/说明）
- stub_identity_t 字段表（32 字节，含注入时机）
- Python 端偏移推导公式（不硬编码，从 config.h 解析 STUB_DATA_SIZEOF）
- 变更流程：修改 stub_data_t 时的同步更新清单

### 评估后不改的项

**A1/N9 提取 UI 代码到 common/ui_dialog.h**：两端签名差异太大
（fn 表 vs 直接调用、读全局 vs 传 hdr 参数、字符串位置不同），
强行抽取会引入 #ifdef 嵌套和参数适配层，增加复杂度。

**Q3 封装 loader_ctx_t**：当前全局变量命名清晰（g_reserved_base 等），
单线程 stub 无并发风险。封装为 ctx_t 需改 10+ 函数签名，回归风险高于收益。

### 验证
- clean build 成功（x64 + x86，vswhere 自动探测 VS 路径正常）
- 22/22 e2e 测试通过（11 inplace + 11 reflective）
- 修复过程中发现：stub.c 注释 `*/` 误触发、put_word 作用域错误，均已修复

## 2026-07-22 14:45 代码审查清理（hy3 N2/N3/N5/N8 + ds4p S2）

按外部 code review 低成本高收益清单，清理 5 项问题。所有改动均为局部清理，
不影响加壳/解密/加载主流程。

### 改动列表

**N3 删除死变量**（`inplace/stub.c`）
- 删除 `static volatile int g_tls_decrypted = 0`（全文件无其他引用）

**N2 删除 DontSleep 调试探测代码**（`reflective/loader.c`）
- 删除 patch_peb_ldr() 末尾的 `FindResourceW(L"AAAA_UNICODE.TMP", L"PNG")`
  诊断块（针对 DontSleep 样本硬编码资源名，每次启动都跑，暴露调试信息）
- 同时删除 RT_STRING/RT_MANIFEST fallback 探测代码

**N8 密码路径 CP_ACP → CP_UTF8**（`inplace/builder.c` + `reflective/builder_reflective.c`）
- builder 密码 → UTF-16 转换从 `CP_ACP` 改为 `CP_UTF8`
- 共 5 处：inplace 2 处（test123 + pwd_arg），reflective 3 处（test123 + pwd_arg + hello123）
- 目的：与 stub 端 `utf16le_to_utf8()` 完全一致，避免非 ASCII 密码 hash 不匹配
- `char_to_wchar()` 路径转换仍用 `CP_ACP`（Windows 路径是 ANSI 编码，与文件系统一致）

**N5 删除地址白名单**（`reflective/loader.c`）
- 删除 `reserve_preferred_base_region()` 中 4 值魔法白名单
  （`0x400000 / 0x10000 / 0x140000000 / 0x180000000`）
- 改为用户态合法区间校验：`0x10000 <= base <= 0x7FFFFFFFFFFF0000`
- 直接信任 `hdr->image_base`（已有 size_of_image 合理性校验兜底）
- 修复：自定义 ImageBase 的 PE 不再被静默放弃 reserve

**S2 测试模式加确认提示**（`inplace/builder.c` + `reflective/builder_reflective.c`）
- `-t/--test` 模式在交互式终端（`GetFileType(stdin) == FILE_TYPE_CHAR`）时
  提示 "WARNING: Test mode uses hardcoded password 'test123'. Continue? [y/N]"
- 用户输 `y/Y` 才继续，否则退出 1
- 非交互式（脚本调用，如 e2e 测试）跳过确认，不影响自动化

### 验证
- clean build 成功（x64 + x86）
- 22/22 e2e 测试通过（11 inplace + 11 reflective）
- stub freshness 校验通过

## 2026-07-22 14:05 删除 v1 兼容代码 + check_stub_freshness --strict 模式

按用户要求"开发中不考虑兼容性，去掉 fallback 代码"，系统性删除 v1 明文密码
兼容路径，强制 v2（SHA-256 hash + XTEA 加密）。同时响应外部 code review #5
（check_stub_freshness.py warn-only 等于没有），添加 --strict 模式。

### 改动列表

**inplace stub：强制 SHA-256 hash 校验**
- `common/config.h`：删除 `stub_data_t.password[64]` 字段和 `STUB_FLAG_HASH` 宏，
  `STUB_DATA_VERSION` 5→6，`STUB_DATA_SIZEOF` 320→192
- `inplace/stub.c`：`verify_password()` 删除明文分支（`flags & 0x1`），
  删除 `wstr_eq()` 函数（不再引用），删除 `.password` 初始化
- `inplace/builder.c`：`sd->flags = STUB_FLAG_HASH` → `sd->flags = 0`，
  删除 `wcsncpy(sd->password, ...)`，密码以 SHA-256 hash 存储

**reflective loader：强制 v2 XTEA 加密**
- `reflective/payload.h`：删除 `reflective_payload_t.password[32]` 字段和
  `RFLAG_HASH` 宏，`REFLECTIVE_PAYLOAD_VERSION` 1→2，
  删除 `REFLECTIVE_PAYLOAD_VERSION_V2`（v2 已是唯一版本），
  `REFLECTIVE_PAYLOAD_HEADER_SIZE` 216→152
- `reflective/builder_reflective.c`：删除整个 v1 明文 `else` 分支，
  无密码时用默认 "hello123"（不再报错），`hdr->flags = RFLAG_ENCRYPTED`（不再设 RFLAG_HASH）
- `reflective/loader.c`：
  - `verify_password()`：删除 `if (!(hdr->flags & RFLAG_HASH))` 明文分支
  - `decrypt_payload_if_needed()`：删除 v1 明文检查
  - 版本校验：只接受 `REFLECTIVE_PAYLOAD_VERSION`（v2）
  - 早期 reserve 函数：删除 v1 读 PE 头的 else 分支，直接用 hdr 字段
  - `map_image()`：更新注释（payload_data 已解密，直接读 PE 头）
  - DBG 日志：`MVP v1` → `v2 (XTEA + SHA-256)`

**check_stub_freshness.py：添加 --strict 模式（review #5）**
- 默认行为不变（warn-only，返回 0）
- `--strict` 标志：CRC 不匹配时返回 1，让 CI 能 fail
- 文档说明退出码含义

### 验证
- clean build 成功（x64 + x86 全部编译通过）
- patch_stub_identity.py 正确识别新结构（v6, 192 bytes）
- 22/22 e2e 测试通过（11 inplace + 11 reflective，含 x64/x86/MinGW/MSVC/UCRT/MFC/GUI 样本）
- stub freshness 校验通过（source_crc 匹配）

### 外部 code review 处理状态
- #5（check_stub_freshness warn-only）：**已修复**（添加 --strict）
- #6（inspect_stub.py PE 解析 TODO）：**过时**（PE 解析已实现）
- #11（MinGW x86 stub 行为差异）：**过时**（build.ps1 默认 MSVC）
- #1/#2/#3/#4/#7/#8/#9/#10：设计权衡，暂不改动

## 2026-07-22 17:00 代码审查：packer-code-review-by-ds4p-20269722b.md

由 DeepSeek-v4-Pro 对 `packer/` 全部源码进行架构/代码质量/逻辑/流程/扩展性/测试
全面审查，输出 `packer/docs/packer-code-review-by-ds4p-20269722b.md`。

关键发现：
- P0: builder 与 stub 密码编码不一致（ACP vs UTF-8），非 ASCII 密码会 hash 不匹配
- P1: `put_dword`/`put_word` 弹框构造 hack 脆弱，建议改用 linker 方案
- P1: `builder.c` main() 过长（870 行），建议拆分
- P2: 全局状态过多（7 个 static 全局变量），建议封装为 context 结构体
- P2: OPNELSHADOW 替换 OH() 宏实现（依赖编译器扩展）
- 共 20 条建议，按 P0/P1/P2/P3 分级

## 2026-07-22 13:40 MSVC 移植阶段 5 完成：节名伪装 + 特征消除

按 `packer/docs/MSVC_PORRINT_AND_PIC_SPEC.md` 阶段 5 实施，完成节名伪装与
packer 特征消除四项改动。`build.ps1` 默认禁用 MinGW 构建作为前置改动，
确保后续验证都基于 MSVC 产物。

### 改动列表

**前置：build.ps1 默认禁用 MinGW 构建**
- `-SkipMinGW` 开关参数改为 `-UseMinGW`，默认跳过 MinGW 构建
- 仅在显式 `-UseMinGW` 时构建 MinGW 产物（更小但已不是主路径）
- 原因：inplace stub 全面迁移到 MSVC，避免测试时仍用 MinGW stub

**5.1 节名伪装：.lock → .text2**
- `common/config.h`：`WINLOCK_SECTION_NAME` 从 `".lock\0\0\0"` 改为 `".text2\0\0"`
- `common/winlock_compat.h`：MSVC 节名 `.lock$X` → `.text2$X`（text/data/rdata/entry/tlscbm/tlscb）
- `common/peb_walk.h`、`common/sha256.h`、`common/xtea.h`：节名同步更新
- `inplace/stub.ld`：所有 `.lock.*` KEEP() 规则改为 `.text2.*`
- `inplace/stub_asm_x64.asm`、`inplace/stub_asm_x86.asm`：MASM SEGMENT ALIAS 改为 `.text2$text` / `.text2$tlscb`
- `inplace/builder.c`：`strcmp(name, ".lock")` 改为 `strcmp(name, ".text2")`
- `cmake/extract_lock_section.py`：`s["name"] == ".lock"` 改为 `".text2"`
- `cmake/inspect_stub.py`：`name[:5] == b".lock"` 改为 `name[:6] == b".text2"`
- `build.ps1`、`Makefile.mingw`：objcopy `-j .lock` 改为 `-j .text2`
- 全部源码文件注释中的 `.lock` 同步更新为 `.text2`（保留"原名 .lock"历史说明）
- 效果：inplace 产物 PE 节表包含 `.text2` 节而非 `.lock` 节，降低启发式可疑度

**5.2 .text2 节去除 W 属性（ERC）**
- `inplace/builder.c`：新节 Characteristics 从 `ERWC (0xE0000020)` 改为 `ERC (0x60000020)`
  （移除 `IMAGE_SCN_MEM_WRITE`）
- `inplace/stub.c`：stub_entry 新增 step 1.5 —— PEB walk 解析 VirtualProtect，
  把 fn 表所在页临时翻转为 RWX（写完函数指针后由系统恢复）
- 用 `PAGE_EXECUTE_READWRITE` 而非 `PAGE_READWRITE`：fn 表所在页可能含 .text2 代码
- 效果：静态分析看 PE 节表无 W+X packer 特征，运行时 stub 仍能写 fn 表

**5.3 PE CheckSum 重算**
- `inplace/builder.c`、`reflective/builder_reflective.c`：
  写文件前调 `CheckSumMappedFile`（imagehlp.lib）重算 PE CheckSum
- `inplace/CMakeLists.inc`、`reflective/CMakeLists.inc`：链接 `imagehlp`
- 效果：packed EXE 的 `OptionalHeader.CheckSum` 非零，消除"packer 不算 checksum"特征

**5.4 Reflective 假 IAT 注入**
- `reflective/loader.c`：新增 `dummy_iat_padding()` 引用 ShellExecuteW / CoInitialize /
  InitCommonControls，链接 shell32/ole32/comctl32
- `reflective/CMakeLists.inc`：stub_reflective_${ARCH} 链接 user32/kernel32/shell32/ole32/comctl32
- 效果：reflective 产物 IAT 包含 5 个 DLL（USER32/KERNEL32/SHELL32/ole32/COMCTL32），
  看起来像正常 GUI 程序，而非过干净的 kernel32+user32

### 验证

- Clean build: `pwsh -File build.ps1 -Clean` 全部成功（x64 + x86）
- Auto e2e test: **22/22 全部通过**
  - inplace: hellomingw, hellomingw32, helloucrt, helloclivsx86/x64,
    helloguix86/x64, hellomfcx86/x64, Notepad4, DontSleep
  - reflective: 同上 11 个样本
- 节名验证（pefile）：inplace 产物均有 `.text2` 节，reflective 产物均有 `.rdata2` 节，
  所有产物均无 `.lock` 节

## 2026-07-22 12:50 auto_e2e_test 新增 VS 样本修复 + 误导性错误消息改进

### 修复

**新增 helloclivsx86/x64 样本后 PACK_FAIL + identity mismatch 警告 + 误导性错误消息**

问题 1：新增 VS 构建的 CLI 样本加壳失败
- 根因：测试脚本写 `helloclivs86.exe`/`helloclivs64.exe`，实际文件名是
  `helloclivsx86.exe`/`helloclivsx64.exe`（多了 `x`），`Test-Path` 失败
- 修复：[auto_e2e_test.ps1](file:///C:/Home/Projects/applocker/packer/tests/auto_e2e_test.ps1)
  改正样本名

问题 2：hellomingw32.exe identity mismatch 警告
- 根因：`Verify-PackedIdentity` 用文件名猜架构（`$PackedExe -match "x86"`），
  但 `hellomingw32.exe` 不含 "x86" 字符串，被误判为 x64，对比了错误的 stub.bin
- 修复：改为先读 packed.exe 的 identity，用其中的 `stub_arch` 字段（1=x86, 2=x64）
  选对应的 stub.bin，不再猜文件名

问题 3：PACK_FAIL 错误消息误导性
- 之前：`[FAIL] 加壳失败 (rc=-1)` — 不显示具体原因（文件找不到 vs builder 错误）
- 现在：`[FAIL] 加壳失败: sample not found: <路径>` 或 builder 的 stderr 输出
- 同时修复 CUI CRASH 丢失 stdout、OUT_FAIL 不显示实际输出的问题
- `Pack-Sample` 的 "sample not found" 也补上了文件路径

验证：auto e2e test 22/22 全部通过

## 2026-07-22 09:30 MSVC inplace stub TLS_PROXY 模式修复（marker/function 节分离）

### 修复

**MSVC 构建的 inplace stub 在有 TLS callbacks 的样本上崩溃 0xC0000005**

现象：
- MSVC 构建的 inplace stub（无 MinGW 覆盖）在 hellomingw.exe / helloucrt.exe 上崩溃
  0xC0000005（这两个样本有 TLS callbacks，触发 TLS_PROXY 模式）
- hellocli.exe（无 TLS）正常，其他 GUI 样本（Notepad4、DontSleep）也正常
- reflective stub 不受影响（用不同的 TLS 处理方案）

根因：MSVC link.exe 不合并不同 flag 的 `$` 子节
- `.lock$tlscbm` 是 const data 段（flags 0x40000040，只读不可执行）
- `.lock$tlscb` 是 code 段（flags 0x60000020，可执行）
- 两者 flag 不同，link.exe 不会把它们合并到同一个输出节
- 导致 MSVC stub.exe 出现 4 个独立 .lock 节，marker 和 function 分到不同节
- [builder.c](file:///C:/Home/Projects/applocker/packer/inplace/builder.c) 的
  `find_stub_tls_cb_offset` 假设 `function = magic + 16`，但 magic+16 处是节末填充（全零）
- builder 把错误地址写入 TLS callbacks 数组，OS loader 调用错误地址 → 0xC0000005

修复方案：用 .asm 文件把 marker 和 function 放进同一个 `.lock$tlscb` SEGMENT
- [stub_asm_x64.asm](file:///C:/Home/Projects/applocker/packer/inplace/stub_asm_x64.asm)：
  新增 `locktlscb SEGMENT READ EXECUTE ALIAS('.lock$tlscb')`，包含 marker + stub_tls_callback
- [stub_asm_x86.asm](file:///C:/Home/Projects/applocker/packer/inplace/stub_asm_x86.asm)：
  同上，x86 版本用 `ret 12`（stdcall 3 个 DWORD 参数），符号名 `_stub_tls_callback@12`
- [stub.c](file:///C:/Home/Projects/applocker/packer/inplace/stub.c)：
  用 `#ifndef _MSC_VER` 包裹 marker 和 stub_tls_callback 的 C 定义
  （MinGW 仍用 C 定义 + stub.ld 的 KEEP() 保证顺序）
- [CMakeLists.inc](file:///C:/Home/Projects/applocker/packer/inplace/CMakeLists.inc)：
  添加 `/INCLUDE:stub_tls_callback`（x64）或 `/INCLUDE:_stub_tls_callback@12`（x86）
  强制保留 stub_tls_callback（未被 stub.c 直接引用，/OPT:REF 会移除）

验证：
- stub.bin 布局验证：magic+16 处是 stub_tls_callback 的 ret 指令
  - x64: `C3` (ret)
  - x86: `C2 0C 00` (ret 12)
- auto e2e test 全部通过：18/18（9 inplace + 9 reflective）
  - 包括之前失败的 hellomingw.exe / helloucrt.exe（TLS_PROXY 模式）
- 现在 MSVC 构建的 inplace stub 完全可用，不再需要 MinGW 覆盖

影响：packer 的 MSVC 迁移工作完成，inplace stub 可完全用 MSVC 构建
